class RobotParameters(object):

    def __init__ (self, maxWheelSpeed, wheelRadius, robotWidth):
        self.maxWheelSpeed = maxWheelSpeed
        self.wheelRadius = wheelRadius
        self.robotWidth = robotWidth